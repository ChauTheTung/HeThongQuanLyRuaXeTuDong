Thư mục `database` để lưu các file liên quan đến cơ sở dữ liệu (ví dụ: SQLite DB, SQL dumps, backup).

Lưu ý:
- Không commit các file DB nhạy cảm vào repository công khai.
- Nếu cần, thêm `database/*.db` vào `.gitignore`.
